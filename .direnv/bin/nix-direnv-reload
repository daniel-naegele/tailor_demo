#!/usr/bin/env bash
set -e
if [[ ! -d "/home/daniel/code/rautemusik/tailor_demo" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/home/daniel/code/rautemusik/tailor_demo")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/home/daniel/code/rautemusik/tailor_demo" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/home/daniel/code/rautemusik/tailor_demo/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/home/daniel/code/rautemusik/tailor_demo/.envrc" "/home/daniel/code/rautemusik/tailor_demo/.direnv"/*.rc
